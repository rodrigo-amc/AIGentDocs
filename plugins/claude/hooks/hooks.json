{
  "description": "AIGentDocs lint feedback on Stop: in a project documented with the standard (docs/project/ exists) and with the aigentdocs CLI installed locally, run the deterministic lint and surface critical findings before finishing. Inform-don't-block: disable with AIGENTDOCS_STOP_LINT=off.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/stop-lint.sh"
          }
        ]
      }
    ]
  }
}
