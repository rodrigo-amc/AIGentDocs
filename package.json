{
  "name": "aigenticdocs-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "AIGenticDocs: docs-as-code standard for AI-augmented software engineering, plus its tooling (CLI and MCP server).",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test packages/core/dist/test/ packages/cli/dist/test/",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "typescript": "^5.5.0"
  }
}
