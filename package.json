{
  "name": "aigentdocs-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "AIGentDocs: docs-as-code standard for AI-augmented software engineering, plus its tooling (CLI and MCP server).",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync-standard": "node scripts/sync-standard.mjs",
    "build": "npm run sync-standard && tsc -b",
    "test": "npm run build && node --test packages/core/dist/test/ packages/cli/dist/test/ packages/mcp/dist/test/",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "typescript": "^5.5.0"
  }
}
